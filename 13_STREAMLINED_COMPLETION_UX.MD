# 📋 13_STREAMLINED_COMPLETION_UX.MD — البروتوكول الرسمي لـ Phase 39
## Streamlined Completion Card & Productive Accounts Filtering

> **الحالة:** Baseline معتمدة قبل أي تعديل كود — نقطة استئناف مرجعية ثابتة في Git
> (استكمالاً لسلسلة `11_Cap.MD` و`12._Recovery.MD`).
> **التاريخ:** 2026-08-22 — الجلسة S60 | **القرار المرجعي:** DEC-034

---

## 1️⃣ الرأي الهندسي الصريح (Architectural Opinion)

### ✅ ما أوافق عليه بقوة
1. **حذف التكرار الأعمى صحيح 100%** — الفحص الميداني أثبت أن
   `latest_line` (سطر 6471) يطبع `context.get('latest_pid') or pid` بينما
   `pid` نفسه (سطر 6493) هو `context.get("current_pid") or final_pid...` —
   في المسار الطبيعي **نفس القيمة حرفياً**. وكذلك `resume_line` يكرر
   `url_line`. حذفهما صفر خسارة معلوماتية.
2. **الفلترة الذكية تصميم سليم** — الحسابات التي فتحت span وأُغلق خلال
   ثوانٍ (نفاد رصيد فوري/pre-flight skip) لم تولّد شيئاً؛ عرضها ضجيج خالص.
3. **الحفاظ على القائمة الرأسية المفلترة مع الأدوار** (البداية/استئناف N/
   🌟 الحساب المنجز) يحل مشكلة سطر الأسهم الأفقي الذي يستهلك 800+ حرف
   بـ 30 إيميلاً — ويخفف ضغط ميزانية P34 (سقف 3500) فتكبر مساحة المعاينة.

### ⚠️ تحفظات هندسية وقرارات التصميم الدفاعية
1. **عتبة الفلترة يجب أن تكون ثابتاً مركزياً** `PRODUCTIVE_SPAN_MIN_SECONDS = 60`
   (دقيقة واحدة) — الأمثلة الميدانية للمالك: المستبعَدون 1–11 ثانية،
   والمنتجون 6+ دقائق. الفجوة واسعة والدقيقة حد فاصل آمن. ممنوع hardcoding
   متناثر (نمط P34/P32).
2. **Fail-Open إلزامي:** لو الفلترة أفرغت القائمة كلها (مهمة سريعة جداً
   شرعية < 60 ثانية أنجزت فعلاً) ➔ **نعرض القائمة الكاملة بلا فلترة**.
   قاعدة: «الفلترة تنظيف لا إخفاء» — بطاقة بلا أي حساب أسوأ من بطاقة مزدحمة.
3. **الحساب المُنجِز محصَّن من الفلترة:** آخر حساب في الرحلة (صاحب آخر span)
   يظهر **دائماً** حتى لو مدته < 60 ثانية — هو من سلّم النتيجة النهائية
   وإخفاؤه يكسر المعلومة الذهبية.
4. **الإجماليان يُحسبان من المفلتَر فقط:** «إجمالي زمن التوليد» = مجموع
   الحسابات المنتجة (المالك اعتمد 38 د 24 ث لا 39 د 13 ث في مثاله) —
   بينما «الزمن الكلي للمهمة» (wall clock) يبقى كما هو لأنه حقيقة زمنية
   مستقلة عن الفلترة.
5. **عداد الاستئنافات لا يُشتق أبداً من عدد الحسابات** — عقد P30 الصارم
   (`Resume ≠ Accounts−1`) يبقى: المصدر الوحيد `last_credit_continuations`.
6. **البيانات الكاملة لا تُفقد:** القائمة غير المفلترة (كل الحسابات بمددها)
   تُسجَّل في **اللوج** (`log_event info`) لحظة بناء البطاقة — الشات نظيف
   واللوج جنائي كامل (فلسفة P30 Forensic).
7. **علم الانتهاء لا يُحذف من المنطق:** `is_finished` يُحذف **من نص
   الرسالة فقط**؛ الدالة `check_project_finished_flag` وموقعها في p05
   يبقيان (تُستعمل مستقبلاً ومختبرة) — لكن استدعاءها في worker يبقى فقط
   إن بقي له استخدام فعلي، وإلا يُحذف الاستدعاء اليتيم مع سطر العرض.
   ✔️ قرار نهائي بعد الفحص: الاستدعاء في السطر 6435 يُستعمل حصرياً لسطر
   العرض 6490 ➔ **يُحذفان معاً** (الدالة نفسها باقية في p05 بحراسها).
8. **`format_account_journey_line` (P29) تبقى كدالة** — يُحذف حقنها في
   بطاقة الاكتمال فقط (سطر الأسهم)، لأن القائمة الرأسية المفلترة تغني
   عنه. حِرفية عقد P38 (`</code>{journey_block}`) ستتغير ➔ تحديث حارسَي
   P38/P29 المعنيين **بوعي** لا حذفاً أعمى: يتحول العقد إلى
   `f"📧 <b>الحساب:</b> <code>{acc_email}</code>\n"` (بلا journey_block).
9. **Edge Cases مغطاة بالحراس:** spans فارغة ➔ "" | كل الحسابات قصيرة ➔
   Fail-Open | عودة A→B→A بمجموع فترات عابر للعتبة ➔ يُحتسب المجموع
   المجمَّع لا الفترة المفردة | إيميلات بـ HTML ➔ تهريب | مدد سالبة ➔ 0.

### 🎯 اقتراحات إضافية معتمدة ضمن النطاق
- ترقيم القائمة يبقى بعد الفلترة (1..N المفلتر) — أنظف بصرياً.
- تسمية القسم تتغير إلى «📊 الحسابات الفعلية التي قامت بالتوليد والاستئناف»
  عند تفعيل الفلترة، وتبقى «📊 إحصائيات الحسابات وزمن التشغيل» في وضع
  Fail-Open — تمييز صادق للمستخدم.
- سطر الإجمالي: `⏱️ إجمالي زمن التوليد: X (N حسابات منتجة | 🔁 M استئناف)`
  — دمج ثلاث معلومات في سطر واحد (بدل 3 أسطر P30 القديمة).

---

## 2️⃣ المواصفات المثبتة بالفحص الميداني (INSPECT-VERIFIED)

### 🗑️ المحذوفات من `res_msg` في `01.33` (المواقع الفعلية — p11)
| السطر | المحتوى | القرار |
|---|---|---|
| 6471 | `latest_line` (🧷 Latest Project ID) | حذف التعريف والحقن |
| 6473–6474 | `resume_line` (🔗 رابط الاستئناف الحالي) | حذف التعريف والحقن |
| 6476–6477 | `fork_line` (🔀 سياق المشروع...) | حذف التعريف والحقن |
| 6484 | `📁 مسار الساندبوكس` | حذف السطر من f-string |
| 6490 | `🏁 علم الانتهاء` | حذف السطر + استدعاء `is_finished` (6435) |
| 6430–6431 | `journey_line`/`journey_block` (🧾 مسار الحسابات) | حذف الحقن (الدالة باقية) |

> ⚠️ بطاقات handoff (6250–6255) واللقطة/المزامنة (6323–6324) فيها
> Root/Latest IDs **مختلفان فعلياً** لحظة الـ fork — **خارج نطاق P39**
> (التكرار هناك معلومة حقيقية لا حشو). النطاق حصرياً بطاقة الاكتمال/الرفض.

### ✅ الباقي في البطاقة (بالترتيب)
1. `outcome['title']` + المعاينة `response_preview` (عقود P34 كاملة بلا مساس)
2. `🧭 النتيجة النهائية` + `url_line` (🌐)
3. `📊 الحالة` + `📌 اسم المشروع` + `🔐 مفتاح المشروع`
4. `📧 الحساب` (عقد P38 — **بلا** journey_block بعد P39)
5. `🆔 Project ID` + `root_line` (🌱 Root Project ID)
6. كتلة الإحصائيات المفلترة الجديدة (أدناه)
7. الكيبوردات كما هي (P33/P35/P37 بلا أي مساس)

### 📊 كتلة الإحصائيات الجديدة — `format_account_timing_block` (p03)
```text
📊 الحسابات الفعلية التي قامت بالتوليد والاستئناف:        ← عند تفعيل الفلترة
  1. <code>email_1</code> — ⏱ 15 دقيقة و34 ثانية (البداية)
  2. <code>email_2</code> — ⏱ 16 دقيقة و36 ثانية (استئناف 1)
  3. <code>email_3</code> — ⏱ 6 دقائق و14 ثانية (🌟 الحساب المنجز)
⏱️ إجمالي زمن التوليد: 38 دقيقة و24 ثانية (3 حسابات منتجة | 🔁 2 استئناف)
🕒 الزمن الكلي للمهمة: 39 دقيقة و13 ثانية
```
قواعد الأدوار: أول مفلتر ➔ `(البداية)` | الأخير (المُنجِز الحقيقي من آخر
span خام) ➔ `(🌟 الحساب المنجز)` | الأوسط ➔ `(استئناف k)` بترقيم تسلسلي.
لو المُنجِز هو نفسه الأول (حساب واحد) ➔ `(🌟 الحساب المنجز)` تتغلب.
تجميع A→B→A: مدخل واحد بمجموع الفترتين + `×2` (سلوك P30 محفوظ).

### 🔧 الدوال والثوابت الجديدة/المعدلة (كلها في p03 بجوار P30)
- **ثابت:** `PRODUCTIVE_SPAN_MIN_SECONDS = 60` (تعريف وحيد top-level).
- **دالة جديدة:** `filter_productive_account_entries(aggregated, last_email)`
  ➔ `(filtered_list, fail_open_flag)` — نقية قابلة للاختبار المعزول.
- **تعديل:** `format_account_timing_block` — يستدعي الفلتر، يبني الأدوار،
  السطر المدمج، ويُبقي `🕒 الزمن الكلي` — توقيعها **لا يتغير**
  (نفس نداء worker السطر 6433، عقد P30 `test_final_message_includes_timing_block` سليم).
- **تسجيل جنائي:** في worker قبل الإرسال: `log_event("info", ...)` بالقائمة
  الكاملة غير المفلترة (best-effort داخل try/except صامت).

### 🛡️ الحراس المتأثرون (حصر نهائي من grep)
| الملف | الحارس | الإجراء |
|---|---|---|
| `test_p30_account_timing.py` | `TestFinalMessageBlock` (9 حالات) | تحديث: العنوان الجديد/الأدوار/السطر المدمج/الفلترة |
| `test_p30...` | `test_final_message_includes_timing_block` | يبقى (التوقيع لم يتغير) |
| `test_p29_account_observability.py` | `TestFinalJourneyLine` (5) | تبقى (الدالة باقية) — **يُحدَّث** `test_final_message_uses_journey_block` (عقد الحقن أُلغي) |
| `test_p38_unified_account_email_display.py` | حرفيات `{journey_block}` (2–3 حالات) | تحديث للحرفية الجديدة بلا journey_block |
| `test_p37/p33/p34/p35` | — | لا مساس (grep نظيف) |

### 🧪 الحزمة الجديدة `tests/test_p39_completion_ux_streamlining.py`
مجموعات: (1) الثابت والفلتر النقي (عتبة/تحصين المُنجِز/Fail-Open/تجميع
العودة) | (2) الكتلة الجديدة (العنوان/الأدوار/السطر المدمج/الإجمالي من
المفلتر فقط/عداد مستقل) | (3) نظافة `res_msg` بالمصدر (غياب الحرفيات
الست المحذوفة من f-string البطاقة) | (4) Zero-Breaking (بقاء الدوال
P29/P30/P38 والكيبوردات والتصدير للمرايا) | (5) HTML escaping وحواف.

---

## 3️⃣ Deep Thinking Tasks — نقطة الاستئناف المرجعية

- [x] **T0** خط أساس أخضر 761 + Exit 0 + نظافة git المرة 26 (`4b2019a`)
- [x] **T1** فحص ميداني INSPECT-ONLY كامل (المواقع والعقود أعلاه)
- [x] **T2** هذا البروتوكول + commit baseline قبل أي كود
- [ ] **T3** تنفيذ P39 في `01.33`: الثابت + الفلتر + الكتلة الجديدة +
      تنظيف `res_msg` (حذف 6 عناصر) + التسجيل الجنائي في اللوج
- [ ] **T4** تحديث الحراس القدامى (p29×1 + p30×~9 + p38×~3) + الحزمة
      الجديدة `test_p39_completion_ux_streamlining.py`
- [ ] **T5** تحديث `scripts/rebuild_refactor.py` (حدود PARTS + أوصاف p03/p11)
      + إعادة توليد `bridge_refactor/` + parity 11/11
- [ ] **T6** بوابة الجودة: pytest 100% + `hadith_sijil.py` Exit 0
- [ ] **T7** التوثيق: DEC-034 (SESSION_LOG) + TEST_SUITE_CATALOG + README +
      V3_RESUME (قاعدة P39 الدائمة) + نسختا PROGRESS + commit نهائي

**قاعدة الإيقاف:** لو تغيّر `Deep_Thinking_Tasks_Remaining.TXT`
(md5 الحالي `9f8968be6b54b8d6e9a1df104d20f11e`) ➔ توقف فوري بعد
تحديث PROGRESS + commit.
