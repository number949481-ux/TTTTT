# 🧭 PHASE 43-PREP — DOCS ALIGNMENT & RESUME PROMPT BUILD (DEC-039)
# جلسة تجهيز وتوثيق فقط — ممنوع تنفيذ أي كود ميزة في هذه الجلسة
# الريبو: github.com/number949481-ux/TTTTT | الفرع: genspark_ai_developer

━━━━━━━━━━━━━━━━━━━━━━━━━━
0. تفويض المالك ونطاق الجلسة (Session Scope — ملزم)
━━━━━━━━━━━━━━━━━━━━━━━━━━
هذا البرومبت هو توجيه المالك الرسمي. مهمة هذه الجلسة حصرياً:
مراجعة كل شيء ➔ تحديث الوثيقة 17 ➔ تحديث البند 7 ➔ بناء برومبت
الاستئناف ➔ 🛑 توقف إلزامي لموافقة المالك.
- أي BLOCKED-ON-OWNER معلّق ➔ مفكوك بهذا الأمر لنطاق هذه الجلسة فقط.
- ⛔ ممنوع منعاً باتاً في هذه الجلسة: تعديل أي سطر في
  01.33_telegram_gen_bridge.py أو كتابة اختبارات الميزة أو بدء CP3+ —
  حتى لو كل الأدلة جاهزة. التنفيذ له جلسة لاحقة ببرومبت الاستئناف
  المعتمد من المالك.
- المرجع المعتمد الوحيد: 17_FAST_LEAN_MODE_AND_ARTIFACTS_BYPASS.MD
  (جذر الريبو) + قرارات المالك D1–D7 أدناه (تعلو عليها عند التعارض).
- 🚨 أرشيف محظور التنفيذ منه نهائياً:
  17.02_FAST_LEAN_MODE_AND_ARTIFACTS_BYPASS.MD +
  17.03_FAST_LEAN_MODE_AND_ARTIFACTS_BYPASS.MD (مسودة خام ملغّمة
  بالأشباح: fetch_sandbox_archive / p11_worker.py / download_artifacts /
  cmd:toggle_fast_mode / أرقام غير مقاسة) + أي جيست خارجي.
- 🚫 مرفوضات المالك النهائية (لا تدخل أي وثيقة تنفيذية):
  R1: حقل download_artifacts أو أي Invariant بين حقلين (X6 — حقل واحد
      fast_mode فقط).
  R2: جعل الوضع السريع افتراضياً في الـ Wizard (الافتراضي False — D5).
  R3: أي CallbackData بنمط cmd:* للميزة — النطاق المعتمد pset:fastmode:*
      وnamespace زر D6 يُثبت من السورس.

━━━━━━━━━━━━━━━━━━━━━━━━━━
1. السياق الثابت
━━━━━━━━━━━━━━━━━━━━━━━━━━
- الملف الوحيد المعتمد للتعديل مستقبلاً: 01.33_telegram_gen_bridge.py
  (~8212 سطراً) — في هذه الجلسة قراءة فقط (grep/sed) بلا أي تعديل.
- ⚠️ التوثيقات متضاربة (883/883 ↔ 946/946 وP40 ↔ P42) — رقم الحقيقة
  الوحيد = ناتج التشغيل الفعلي في S1. كل رقم مكتوب = هدف تحقق لا حقيقة.
- SSOT الحالة: docs/engineering/PROGRESS.md.
- الأسماء المرجعية المثبتة (2026-08-23 — تُعاد بـ grep في S1):
  download_project_archive (2113) / make_project_always_public (2160) /
  process_user_task_async (6332) / registry.snapshot (3818) /
  build_project_settings_keyboard (5060) / default_project_settings (444)
  / _normalize_project_settings (3092) / router pset:* (5064–5066) /
  on_project_update (6416) / lock الـ registry (3217).

━━━━━━━━━━━━━━━━━━━━━━━━━━
2. قرارات المالك D1–D7 (تُدمج نصاً في الوثيقة 17 في S3)
━━━━━━━━━━━━━━━━━━━━━━━━━━
D1 — الزر بستايل "primary" (الأزرق) عبر make_inline_keyboard وWhitelist
  P11 القائمة {primary, success, danger} (تصحيح X4 — P11 أدخلت style
  في Bot API 9.4 وP33 استخدمت success فعلياً) + إيموجي 🔵/⚪ في النص.
  تحقق من الـ Whitelist بـ grep في S2 أولاً.
D2 — نطاق التخطي (حسم G1↔4.2): يتخطى download_project_archive + فك
  الضغط + snapshot الثقيل فقط، ويُبقي make_project_always_public تعمل
  (تضمن فتح رابط المعاينة عاماً). استثناء وحيد: لو أثبت الفحص بدليل
  Terminal أن الرابط يعمل عاماً بدونها ➔ يجوز تخطيها ويُسجل الدليل
  في الوثيقة 17. لا تخطيها بلا دليل.
D3 — توقيع الدالة الحاكمة (وسيط واحد — حرفياً):
   def should_skip_artifacts_download(project_settings: dict) -> bool:
       gh = project_settings.get("github") or {}
       if bool(gh.get("enabled", False)):
           return False
       return bool(project_settings.get("fast_mode", False))
D4 — موضع خطوة الـ Wizard: يُحسم في S2 من حالات الـ Wizard الفعلية
  (بعد الاسم/الموديل وقبل finalize) بشرط نجاة pending_prompt (P42).
D5 — الافتراضي fast_mode=False للجميع — لا استثناء.
D6 — زر "⬇️ تنزيل الساندبوكس الآن" المتأخر في كارت/تفاصيل المشروع
  السريع: ينفّذ الـ Pipeline الكامل عند الطلب بنفس الدوال القائمة
  (بلا نسخ). namespace أزرار الكارت يُثبت من السورس في S2 + حد 64 بايت
  + رسالة فشل صريحة للجلسة المنتهية + Idempotency.
D7 — Telemetry: سطر لوج FAST_MODE_SKIP project=<key> عند كل تخطٍ —
  أي رقم سرعة يُنشر لاحقاً يكون من قياس فعلي فقط.

━━━━━━━━━━━━━━━━━━━━━━━━━━
3. خطوات الجلسة (S1–S6 بالترتيب — لكل خطوة: دليل Terminal ➔ Commit)
━━━━━━━━━━━━━━━━━━━━━━━━━━
- [ ] S1 — LOCATE & PROVE (قراءة فقط):
  أ) اقرأ: README ➔ docs/engineering/PROGRESS.md ➔ V3_RESUME_SESSION.md ➔
     SESSION_LOG.md (آخر DEC-*) ➔ 00_MASTER_RESUME_PROTOCOL.md (أثبت
     مساره بـ ls) ➔ Deep_Thinking_Tasks_Remaining.TXT (البند 7) ➔
     الوثيقة 17 ➔ 17.02 و17.03 (كأرشيف للمقارنة فقط).
  ب) Baseline بمخرجات حقيقية: python -m pytest tests/ -q +
     python scripts/hadith_sijil.py + python scripts/rebuild_refactor.py
     ➔ الرقم الناتج يحسم تضارب 883/946 ويُعتمد Baseline رسمياً.
     فشل قديم غير متعلق ➔ يُوثَّق ويُتجاوز — ممنوع جلسة إصلاح.
  ج) إعادة إثبات كل مواضع البند 1 بـ grep فعلي + توثيق أي انزياح.
- [ ] S2 — الفحص الجنائي التحضيري (قراءة فقط):
  دليل D2 (هل الرابط يفتح عاماً بلا make_public؟ من الكود/اللوجات) +
  حسم D4 (خريطة حالات الـ Wizard) + إثبات Whitelist الـ style (D1) +
  namespace أزرار كارت المشروع (D6) + مسح كل مستهلكي
  archive_path/stage_dir بعد الاكتمال بأرقام أسطر + قياس زمني لمرحلة
  ما بعد الموديل من اللوجات القائمة إن وُجدت (بلا instrumentation —
  لو القياس يحتاج كود ➔ يُؤجَّل للتنفيذ ويُكتب "يُقاس في CP1").
- [ ] S3 — تحديث الوثيقة 17 (المهمة المركزية):
  دمج D1–D7 نصاً + الحالات الحدية 10–15 (تبديل أثناء مهمة/GitHub بعد
  fast/manifest قديم وجديد/D6 فشل وIdempotency/نجاة pending_prompt) +
  تصحيح X4 بقرار D1 + حسم G1↔4.2 بقرار D2 وأدلة S2 + تحديث أرقام
  الأسطر لأرقام الجلسة + خطة CP0–CP9 التنفيذية والاختبارات الـ 16 —
  الوثيقة تصبح SSOT كاملاً مطابقاً للتنفيذ المخطط. Commit مستقل.
  + إضافة بانر في أول 17.02 و17.03: "⛔ أرشيف تاريخي — ممنوع التنفيذ
  من هذا الملف. المرجع الوحيد: 17_FAST_LEAN_MODE_AND_ARTIFACTS_BYPASS.MD"
- [ ] S4 — تحديث Deep_Thinking_Tasks_Remaining.TXT:
  البند 7 يُحدَّث: المهمة النشطة = PHASE 43 / SPEC_DOC = الوثيقة 17
  (المحدَّثة) / DEC-039 / Baseline المُقاس من S1 / حالة التنفيذ =
  "بانتظار اعتماد المالك لبرومبت الاستئناف". Commit مستقل.
- [ ] S5 — بناء برومبت الاستئناف:
  تحديث بلوك ## 0 في 00_MASTER_RESUME_PROTOCOL.md ببرومبت تنفيذ P43
  كامل (CP0–CP9: Baseline ➔ فحص جنائي ➔ Schema ➔ زر pset:fastmode ➔
  حقن 2609–2616 بترتيب _declined أولاً ➔ زر D6 ➔ Wizard ➔ الاختبارات
  الـ 16 ➔ البوابات والتوثيق) مبنياً على أدلة S1–S2 الفعلية والوثيقة 17
  المحدَّثة — لا على أرقام هذا البرومبت. Commit مستقل.
- [ ] S6 — 🛑 التوقف الإلزامي (RESUME PROMPT APPROVAL GATE):
  بعد Commit الـ S5: توقف تام + تقرير للمالك يتضمن نص برومبت
  الاستئناف الجديد كاملاً حرفياً + ملخص ما تغيّر في الوثيقة 17 والبند 7.
  ممنوع بدء أي تنفيذ (CP0+) قبل رد المالك:
  - المالك يقول "تمام" ➔ الجلسة التالية تنفّذ من برومبت الاستئناف.
  - المالك يطلب تعديلاً ➔ عدّل البرومبت ➔ Commit ➔ توقف وأبلغ من جديد.

━━━━━━━━━━━━━━━━━━━━━━━━━━
4. قاعدة بوابة الموافقة الدائمة (تُكتب نصاً في البروتوكول نفسه)
━━━━━━━━━━━━━━━━━━━━━━━━━━
تُضاف للـ 00_MASTER_RESUME_PROTOCOL.md قاعدة دائمة:
"أي جلسة (حالية أو مستقبلية) تجد نفسها بحاجة لتحديث برومبت الاستئناف
في بلوك ## 0 ➔ تحدّثه ➔ تعمل Commit ➔ تتوقف فوراً وتبلغ المالك بالنص
الجديد كاملاً ➔ لا تنفيذ من البرومبت المحدَّث قبل موافقة المالك الصريحة.
استثناء وحيد: التحديث الختامي في نهاية مهمة مكتملة (تسجيل المهمة
التالية) — يُبلَّغ في التقرير النهائي بلا توقف إضافي."

━━━━━━━━━━━━━━━━━━━━━━━━━━
5. TASK CHANGE GUARD
━━━━━━━━━━━━━━━━━━━━━━━━━━
- تحديثات S3/S4/S5 الناتجة عن هذه الجلسة نفسها ≠ تغيير مهمة — لا توقف.
- التوقف فقط لو تغيّر محتوى مهمة المالك لمهمة مختلفة أثناء التنفيذ
  ➔ STOP + توثيق + انتظار توجيه.

━━━━━━━━━━━━━━━━━━━━━━━━━━
6. تقرير الجلسة النهائي — بهذه العناوين حرفياً
━━━━━━━━━━━━━━━━━━━━━━━━━━
STATUS ➔ BASELINE (الرقم المُقاس الحاسم لـ 883/946) ➔ EVIDENCE ➔
DOC-17 CHANGES ➔ TASKS-REMAINING CHANGES ➔ RESUME PROMPT (النص الكامل
حرفياً) ➔ GIT ➔ AWAITING OWNER APPROVAL

━━━━━━━━━━━━━━━━━━━━━━━━━━
7. القواعد الحاكمة (Non-Negotiable)
━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Evidence-First: أي ادعاء بلا مخرجات Terminal = غير منجز.
2. هذه جلسة توثيق — صفر تعديل على 01.33 (قراءة فقط).
3. لا شبح: fetch_sandbox_archive / p11_worker.py / download_artifacts /
   cmd:toggle_fast_mode / أي رقم سرعة غير مقاس — لا تظهر في أي وثيقة
   تنفيذية (وجودها في 17.02/17.03 الأرشيفيين فقط مقبول تحت البانر).
4. أي التباس ➔ BLOCKED-ON-OWNER في PROGRESS.md بدل الاجتهاد.
5. الجلسة تنتهي إجبارياً عند S6 — نجاحها = تقرير + برومبت استئناف
   بانتظار الموافقة، وليس تنفيذاً.

ابدأ الآن: S1 ➔ S2 ➔ S3 ➔ S4 ➔ S5 ➔ 🛑 S6 (توقف + إبلاغ المالك)
