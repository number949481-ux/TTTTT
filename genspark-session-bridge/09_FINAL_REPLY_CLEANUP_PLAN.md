# 🧼 09_FINAL_REPLY_CLEANUP — خطة تنظيف الرد النهائي (تقييم gist وكيل خارجي)

> المرجع الخارجي: https://gist.github.com/pijsal1-tech/8fb7fd76aa49e1780c3a9b8286bd3eea
> (Architecture Brief من وكيل آخر — وصل عبر المالك 2026-08-30)

## 🎯 الهدف المشترك
رسالة تيليجرام النهائية = رد المساعد النظيف 100% (`role: assistant`) بدون:
ضجيج الأدوات (tool wrappers) / بقايا CoT (`<thought>`) / JSON خام / تكرار Stream.

## ⚖️ حكم الفحص (Fact-Check ضد الكود الفعلي — 2026-08-30)
| ادّعاء الـ gist | الواقع عندنا | الحكم |
|---|---|---|
| الرد بيتلوث بـ `<thought>` من SSE | المحرك بيجمّع فقط `message_field_delta` بـ `field_name=="content"` — لا أثر لوسوم thought في الريبو | ⚠️ بلا دليل — يحتاج عيّنة حية |
| فلترة بحقل `is_tool_call` | الحقل غير موجود في أي ملف — الحقيقية: `tool_calls`/`tool_call_id`/`role` | ❌ اسم حقل غالباً متخيَّل |
| سقف 3500 في p11_worker | منفَّذ من P34: `enforce_completion_message_budget` (p11 سطر 339) + `clamp_preview_text` | ✅ تحصيل حاصل |
| السطر 2164/2171 في 01.03 | return فعلي سطر 2171 (01.03) / 1721 (01.04) | ✅ دقيق تقريباً |

**الثغرة الحقيقية المؤكدة:** `fetch_final_reply_text` (p05 سطر ~228 + 01.33 سطر ~1871)
بياخد آخر `role=="assistant"` بلا فحص أنها ليست غلاف أداة أو فارغة.

**حقيقة تشغيلية:** بوت التليجرام يحمّل **01.03 فقط** (01.33 سطر 503) —
أي كلام عن «تجربة حية على 01.04» لا يمس مسار التليجرام مباشرة.

## 📩 المطلوب من الوكيل الخارجي (أرسله المالك)
1. عيّنة JSON خام لرسالة assistant ملوّثة فعلاً (من `session_state.messages` أو dump SSE) — دليل لا افتراض.
2. أسماء الحقول الحقيقية لرسائل الأدوات (`role:"tool"`؟ `tool_calls`؟ `message_type`؟).
3. هل التجربة الحية تمت فعلاً؟ على أي محرك (01.03/01.04)؟ + اللوج/الناتج.
4. رده على سؤالَي الـ gist: تنظيف Post-processing أم فلترة Stream؟ قصّ 3500 أم إرفاق `.txt`؟

## 🛠️ خطة التنفيذ عندنا (بانتظار موافقة المالك)
### المرحلة A — آمنة لا تحتاج دليل:
1. `clean_assistant_reply()` دفاعية في `01.33` + `p05` (لا تعديل للمحرك 01.03 — Golden Baseline):
   إزالة `<thought|thinking|antThinking>...</...>` + بادئات `Assistant:` — no-op لو الوسوم غير موجودة.
2. تصليب `fetch_final_reply_text`: تخطّي assistant الفارغة + ذات `tool_calls`/`role=="tool"`
   (الأسماء الحقيقية — ليس `is_tool_call`) + تمرير الناتج على `clean_assistant_reply` —
   مع بقاء Fail-Open (أي فشل → النص القديم).
3. التعديل في **الملفين معاً** (01.33 + p05) حفاظاً على التطابق + اختبارات P45 جديدة.
### المرحلة B — بعد رد الوكيل:
ضبط regex/أسماء الحقول على العيّنات الحقيقية.
### لن نفعل:
- الاقتراح 3 من الـ gist (السقف 3500) — منفَّذ فعلاً (P34).
- تعديل المحرك 01.03 (قرار Golden Baseline قائم).

## 📌 الحالة
- ~~`STATUS: AWAITING_OWNER`~~ → **`STATUS: PHASE_A_DONE` (2026-08-30 — P45)**

## ✅ ملحق التنفيذ (2026-08-30 — P45)
- رد الوكيل الخارجي وصل في `خطة_استخراج_الرد_النهائي_الصافي_فيب_كودج.md`
  (جذر الريبو): أجاب الأسئلة الأربعة — اعتمد الفلترة بالحقول الحقيقية
  (`tool_calls`/`role`/`content`) وأكّد عدم وجود `is_tool_call` + اعتمد
  Post-processing والقص 3500 القائم (P34).
- **A1 ✅** `clean_assistant_reply()` في `01.33` سطر 1871 (وفي `p05` بإعادة التوليد).
- **A2 ✅** تحصين `fetch_final_reply_text`: تخطي `tool_calls`/الفارغ + تمرير clean
  + Fail-Open لو التنظيف أفرغ الرد (كله CoT).
- **A3 ✅** `tests/test_p45_clean_final_reply.py`: 20 حارساً (+18 subtests).
- خريطة PARTS محدّثة (8616 سطراً) + `bridge_refactor/` معاد توليده — byte-parity سليم.
- المجموعة الكاملة: **1045 passed + 67 subtests — صفر فشل**.
- المتبقي: الاختبار الحي عند المالك (T-GSB-5) + المرحلة B عند الحاجة.
