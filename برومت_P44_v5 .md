# ⚡ PHASE 44 — RESUME & EXECUTE (v5 — Idempotent)

## 📌 المرجع
`18.03_COT_CLEANUP_AND_DYNAMIC_RESUME_PROMPT.MD` = Active Source الوحيد — نفّذه حرفياً.

## 🧭 1) حدد موقعك (قراءة فقط — صفر كتابة)
اقرأ: البند 7 ← بلوك ## 0 ← PROGRESS (جذر + هندسي) ← الوثيقة 18.
- البند 7 ≠ P44 → STOP + BLOCKED-ON-OWNER.
- P44 وCP0 [ ] → أول جلسة: §B ثم **CP0 فقط** ثم Gate-A توقف.
- P44 وCP0 [x] → استأنف من **أول CP غير مكتمل** في البند 7.

## 🧪 2) أثبت الواقع (كل جلسة — يُثبَت لا يُنسَخ)
```bash
python -m pytest tests/ -q          # 962/962 (+12) أو أعلى
python scripts/hadith_sijil.py      # Exit 0
python scripts/rebuild_refactor.py  # Parity 11/11
git ls-files .pytest_cache bridge_bot.log  # فارغ
```
فشل متعلق بـ P44 → أصلحه بدليل. Pre-existing → وثّقه وواصل.

## 🔍 3) فحص «الكود موجود؟» قبل أي CP (يمنع إعادة الشغل)
- لو الكود المطلوب موجود في 01.33 (يُثبت بـ grep) والـ Checkbox لسه [ ]:
  → **التوثيق ناقص فقط**: علّم [x] في البند 7 + الوثيقة 18 + PROGRESS ×2
  + Commit برسالة الـ CP. **ممنوع إعادة كتابة الكود.**
- لو مش موجود → نفّذ الـ CP كاملاً (تنفيذ → دليل Terminal → [x] → Commit مستقل).
- **CP واحد فقط في الجلسة** ثم توقف وقدم تقريرك.

## 🔒 4) القواعد (مخالفة = فشل)
ممنوع لمس 01.03 / Golden Baselines / جسم detect_response_status / P35 / P40 / دلالات P18 (تغليف فقط). ممنوع تفتيت 01.33 (استثناء: tests/test_p44_resume_pipeline_integrity.py). ممنوع sentinel في برومبت المستخدم + dependency جديد + Refactor خارج الـ CP. كل رقم سطر بـ grep -n. غموض معماري → STOP + BLOCKED-ON-OWNER.

## 🧪 5) الاختبارات الـ 12 (عند CP7 — من 18.03 §E)
test_01_gate_holds_running_while_active | test_02_gate_releases_after_two_inactive_reads | test_03_structured_signals_pierce_gate | test_04_gate_neutral_on_network_none | test_05_unstable_reply_holds | test_06_final_fetch_after_p18_stop | test_07_final_fetch_failure_keeps_old_text | test_08_live_rebind_picks_edited_prompt | test_09_rebind_failure_falls_back_to_snapshot | test_10_p20_data_retention_no_rebind | test_11_display_cards_match_sent_prompt | test_12_zero_regression_p35_p40_p18.

## 🧹 6) مزامنة الحوكمة (مع أي CP)
البند 7 ↔ بلوك ## 0 ↔ PROGRESS ×2 ↔ الوثيقة 18 ↔ TEST_EXECUTION_LOGS ↔ rebuild_refactor. تأكد أن الجذري والهندسي متطابقان في P44.

## 📋 7) التقرير بعد كل CP
`STATUS → EVIDENCE → CHANGES → TESTS → GIT → NEXT RESUME POINT`. (CP9: العناوين الـ 12 كاملة.)

## ✅ النهاية
Evidence-First. RESUME لا RESTART. ابدأ: تحديد → إثبات → فحص وجود الكود → نفّذ/وثّق → Commit → توقف.
